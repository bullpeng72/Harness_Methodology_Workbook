# 재현 가능성 계약의 고정 대상 (본편 §2⑦: 결정적 지표만 계약, LLM 생성물은 계약 밖).
# 이 값을 바꾸면 워크북의 실측 수치는 재현 대상에서 벗어난다.

# 모델  (S4: 로컬에 실재하는 모델로 정정 — 이전 핀은 미설치 플레이스홀더였음. IRREVERSIBLE 변경 이력 참고)
tier1: claude-sonnet-5
tier2: exaone3.5:7.8b
tier2_fallback: claude-haiku-4-5-20251001
embed: mxbai-embed-large:latest
seed: 7

# 도구 (본편 §34.4: 착수 시 버전 고정, 업그레이드는 별도 마이그레이션)
# S3 관찰(실습서 부록 J §J.S3): import되는 lib 와 `agent-eval` CLI 가 rc2/rc3로 갈라져
# 있었으나 S8에서 1.0.0 정식으로 정렬. lib·CLI 동일 버전(재현 기준).
agent_evaluator_lib: 1.0.0
agent_evaluator_cli: 1.0.0
