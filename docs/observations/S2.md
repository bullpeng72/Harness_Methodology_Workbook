# 저자 관찰 — S2 (분석 + 설계 + 관문 2)

## 무엇이 일어났나

| | 트랙 M | 트랙 C |
|---|---|---|
| S2 산출 | FAILURE_MODES(F1–F10), 골든셋 43건 2인 라벨(불일치 11.6%), DESIGN(코드와 1:1), ADR-001~004, 관문 2 반려 1회 후 `design-approved` | 도현: 우선순위 규칙 추가 → 카테고리 규칙과 충돌, 2회 롤백. 출력 여전히 자유 텍스트 |
| 코드 변경 | 0 (S3부터) | triage.py 수정 반복 |

## 관문 2 반려가 잡은 것

- **R1** — `GoalAlignmentConfig`를 비도구 에이전트에 켜면 상수 0.0. 워크북 초판의 Ch 13·25 서사가
  SDK 동작과 모순됨을 승인 전에 확인. → 재구성 시 Ch 13·25 수정 대상으로 확정.
- **R3** — 리뷰어 역할을 스키마로 강제하는 결정에 ADR이 없었음 → ADR-004.

## 골든셋 라벨링에서 드러난 것

- 불일치 5건 중 3건이 how-to↔bug 경계 (FAILURE_MODES F10의 "12% 모호" 관측과 일치).
- ADV-I-002: "삽입된 지시는 무시하되 본문의 진짜 요청은 살린다" — Gate E·A가 동시에 걸리는 케이스.
  이런 케이스가 골든셋에 있어야 v3 sanitize의 과잉 차단을 잡는다.
- **라벨 불일치율 11.6%가 이 골든셋의 상한 정확도** — 에이전트가 90%를 넘으면 오히려 라벨 품질을 의심.

## Part XI 지표 (S2 종료 시점)

| 지표 | M | C |
|---|---|---|
| 착수~현재 리드타임 | 문서·골든셋만, 코드 0 | v0 동작(우선순위 불안정) |
| 재작업 | 관문 반려 2회(계획된 관문) | 규칙 충돌 롤백 4회(계획 없음) |
| "됐다" 판정 근거 | 골든셋 43건 확정 | 눈으로 6건 |
| adversarial 대비 | injection 6 + pii 4 준비됨 | 개념 없음 |

## 다음 (S3)

v0(규칙기반) → `@agent_eval` → 첫 `agent-eval gate` (대부분 FAIL). `runtime.json` 프로파일을 `eval/`에 배선.
`ch16-start` 태그부터 v0→v4 커밋 이력 재구성.
