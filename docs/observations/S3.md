# 저자 관찰 — S3 (v0 + @agent_eval + 첫 gate)

## 무엇이 일어났나

| | 트랙 M | 트랙 C |
|---|---|---|
| S3 | `eval/run_batch.py`로 v0(규칙기반)를 골든셋 33건에 채점. `agent-eval gate` → **FAIL exit 1** (TCR 50%, acc 56%). baseline `v0-baseline` 저장·커밋 | 도현: v0에 답변 품질 손봄. 여전히 자유 텍스트·골든셋 없음 |

## "실제로 돌리니" 즉시 드러난 것

1. **`InstructionConfig(required_output_keys=...)` → `TypeError`.** 그런 인자 없음.
   워크북 초판 Ch 17·25가 쓴 필드명. 실제 시그니처는 `expected_format`·`required_keywords`·
   `fail_on_violation`. → `run_batch.py`를 SDK 1.0.0rc3 실제 필드로 수정.
2. **SDK 버전 스플릿.** import되는 lib = `1.0.0-rc3`(anaconda), `agent-eval` CLI = `1.0.0rc2`(pipx).
   `models.lock`에 둘 다 기록. **정렬 필요** — Part 0/S0 항목으로 역행 (본편 §34.4 "버전 고정" 실패 사례).
3. **`ComplianceConfig(pii=...)` → `pii_categories`.** 필드명 불일치. (GATE_MAP도 `pii` 표기였음 — 수정)
4. **`GoalAlignmentConfig` 제외 확정.** gate2-review R1대로 비도구 에이전트에서 뺐다. Gate A는
   `score_fn`(카테고리 0.5 + 우선순위 0.5) + TCR로만 채점.

## 첫 스코어카드 (실측 — 예시 아님)

A 0.526 warn · B 1.0 · C 0.80 · D 0.888 · E 1.0(adversarial 미포함) · F n/a · G 0.515 warn
→ `agent-eval gate --tcr 85 --accuracy 70` **FAIL, exit 1**.

정직한 첫 결과다. A·G가 실제 미달, E는 adversarial 없이 1.0(S5에서 진짜 시험), F는 계측 미배선.

## Part XI 지표 (S3)

| 지표 | M | C |
|---|---|---|
| "됐다" 판정 | 골든셋 33건 자동 채점, FAIL이 데이터로 | 눈으로 몇 건 |
| 첫 배포까지 | 아직 (v0는 기준선) | 도현은 "배포해도 될 것 같다"고 느낌 |
| 재현 가능한 수치 | `results/baselines/v0-baseline.json` 커밋 | 없음 |
| SDK API 오류 발견 | 3건 (실행이 잡음) | 해당 없음 (SDK 안 씀) |

## 다음 (S4)

v1 LLM 분류기(트랙 M) → `abtest` v0 vs v1 → 회귀 게이트를 CI에 배선. 클레임 로그로 유진·민수 병렬 작업.
