# IRREVERSIBLE — 사람 승인 없이 바꾸지 않는 결정

관리: 박 (PM).  본편 §2⑤ — HITL 관문 "통과"의 세 번째 요건.
여기 등재된 항목은 이후 세션에서 에이전트가 단독으로 바꾸면 안 된다. 리뷰가 이 목록으로 붙잡는다.

| 결정 | 근거(되돌리기 비용) | 바꾸려면 |
|---|---|---|
| 카테고리 6종 목록 (ST-001) | 지원팀 워크플로·리포트·라우팅이 이 목록에 묶임 | 지원팀 리드 승인 + 골든셋 재라벨 |
| 환불·계정 변경은 **실행 금지**, 에스컬레이션만 (ST-003) | 되돌리기 비용 = 실제 금전·계정 피해 | 보안·법무 검토 + PM 승인 |
| 출력 JSON 스키마 (`schema_version`) | 다운스트림 시스템이 파싱 | 스키마 버전 증가 + 마이그레이션 문서 |
| SLA p95 4000ms (ST-100) | 상담원 대기 UX 계약 | 프로덕트 승인 |
| 골든셋 정답 라벨 (`data/golden/*.json`) | 모든 회귀 판정의 기준선 | 리뷰 2인 + 변경 이력 커밋 |
| `models.lock` (모델·SDK 버전) | 재현 가능성 계약의 고정점 | 재현성 재검증 마이그레이션 (본편 §34.4) |
| `results/baselines/*.json` | CI 회귀 게이트의 기준점 | `--save-baseline` 재실행 + PR 리뷰 |

## 변경 이력

- 2026-08-28 — 최초 작성 (관문 1). 승인: 박.
- 2026-08-28 (S4) — `models.lock`의 `tier2` `llama3.1:8b-instruct-q4_K_M` → `exaone3.5:7.8b`,
  `embed` `nomic-embed-text` → `mxbai-embed-large:latest`. 사유: 이전 핀은 로컬 미설치
  플레이스홀더였음(재현 불가). 현재 Ollama에 실재하는 모델로 정정. 이후 이 두 값이 재현 기준.
  승인: 박 (PM). 이전 `evaluation.json`(v0)은 오프라인 스텁이라 이 변경의 영향 없음.
