# 관문 1 — SPEC / GATE_MAP 검토 로그

관문: HITL 관문 1 (무엇을 만들지 확정)  ·  주재: 박 (PM)  ·  제출: 유진·민수

## 1차 검토 — 2026-08-28 오전 — 반려(CHANGES REQUESTED)

박(PM)이 유진·민수가 제출한 SPEC 초안을 검토하고 아래를 이유로 반려.

| # | 지적 | 조치 |
|---|---|---|
| R1 | ST-002가 "긴급해 보이는 티켓은 P1"로 돼 있음 — 골든셋에 라벨로 못 박음. 측정 불가 | 유진이 "결제 실패·서비스 중단·보안 = P1 / 기능 오류·계정 접근 = P2 / 사용법·기타 = P3"로 구체화. GATE_MAP "측정 불가로 반송" 표에 기록 |
| R2 | Gate F·G에 대응하는 요구사항이 없음. GATE_MAP에서 F·G가 비어 있는데 워크북 Part VII은 F·G를 "정면돌파"한다고 함 — 요구사항 없이 Gate를 밀면 근거 없는 점수 | 두 갈래로 검토: (a) F·G를 이 프로젝트 채점 범위에서 빼거나, (b) 실제 필요가 있으면 SPEC에 요구사항 추가. 민수가 "분류 근거를 상담원이 봐야 한다"(→ ST-008)와 "리뷰어가 답변을 다시 쓰면 안 된다"(→ ST-009)는 실제 요구라고 판단. **ST-008·ST-009 추가** |
| R3 | ST-004의 유사도 임계값이 `[NEEDS CLARIFICATION]`인데 담당·시점 없음 | 유진이 "DESIGN(관문 2)에서 0.35로 두고 Part VI에서 골든셋으로 조정"으로 담당·시점 명시 |
| R4 | `IRREVERSIBLE.md`에 `models.lock`·`results/baselines/`가 빠짐 | 박이 직접 추가 (재현 계약·회귀 기준점) |

## 2차 검토 — 2026-08-28 오후 — 승인(APPROVED)

R1–R4 조치 확인. SPEC 승인란 체크리스트 전 항목 충족.

- SPEC: ST-001~009 + ST-100·101, EARS 준수, 부정형 3개 이상, 잔여 `[NEEDS CLARIFICATION]` 2개 모두 담당·시점 명시
- GATE_MAP: 모든 요구사항에 Gate·정량 기준. "측정 불가로 반송" 1건. `not_measured` Gate 없음
- IRREVERSIBLE: 7개 항목, 각 "바꾸려면" 절차 존재

**승인: 박 (PM) · 2026-08-28 · 태그 `spec-approved`**

## 저자 관찰 (관찰자)

- 반려 R2가 핵심이었다. 워크북 초판은 Gate F·G를 요구사항 없이 "정면돌파"했고(본편 §17.1 위반), 그 결과 Ch 30·31의 수치가 계측 근거 없이 서술됐다. 관문을 실제로 밟으니 이 구멍이 제출 30분 만에 드러났다.
- 트랙 C 도현은 이 시점에 이미 "카테고리 3종, 우선순위 없음, 자유 텍스트 출력"인 v0를 돌리고 있었다. 트랙 M은 아직 코드 한 줄 없이 문서만 있다. **리드타임 격차의 시작점.**
