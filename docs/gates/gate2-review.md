# 관문 2 — DESIGN / ADR 검토 로그

관문: HITL 관문 2 (어떻게 만들지 확정)  ·  주재: 박 (PM)  ·  제출: 유진 (설계자)

## 1차 검토 — 2026-08-28 — 반려(CHANGES REQUESTED)

| # | 지적 | 조치 |
|---|---|---|
| R1 | DESIGN §3에서 `GoalAlignmentConfig`를 Gate A로 켜는데, SupportTriage는 v1~v2가 **도구를 호출하지 않는다**. SDK의 `eval_goal_alignment`는 도구 없는 태스크에서 `use_llm_scoring=True`가 아니면 상수 0.0을 반환한다(본편 §17.1 관련 SDK 확인). 이대로면 Gate A에 상수 0.0이 섞임 | 유진이 §3 표에 "`use_llm_scoring=True` 병기, 아니면 켜지 않음" 명시. Ch 13·25(워크북)의 서사도 이 기준으로 재작성 필요 — 저자에게 전달 |
| R2 | ST-009(리뷰어)의 재작성 라운드 상한이 "1"인데 근거가 "느낌"이었음 | 민수가 "2회 이상은 지연·비용만 늘고 boundary 개선 미미할 것으로 판단, S5에서 실측"으로 담당·시점 명시. ADR-004에 반영 |
| R3 | ADR이 3개뿐 — 리뷰어 역할 강제(스키마)가 되돌리기 어려운 결정인데 ADR 없음 | ADR-004 추가 |
| R4 | 에러 정책의 "재시도 1회"가 SPEC/코드와 일치하는지 미확인 | 유진이 `pipeline.py`의 `_MAX_LLM_RETRIES = 1` 확인, DESIGN §5에 명시 |

## 2차 검토 — 2026-08-28 — 승인(APPROVED)

R1–R4 조치 확인. DESIGN 승인란 체크리스트 전 항목 충족.
ADR-001~004 존재. NEEDS CLARIFICATION 2건 모두 담당·시점 명시.

**승인: 박 (PM) · 2026-08-28 · 태그 `design-approved`**

## 저자 관찰

- R1이 중요하다 — 워크북 초판 Ch 13은 "`ignore_no_tool_tasks=False`로 켜라"고만 했고 Ch 25는
  "goal_alignment 0.79→0.89"라고 썼는데, 이 설정으론 그 수치가 움직일 수 없다(SDK 소스 확인).
  관문에서 SDK 동작을 실제로 대조하니 이 오류가 승인 전에 잡혔다.
- 트랙 C 도현: 이 시점에 우선순위를 규칙으로 넣었으나 카테고리 규칙과 충돌해 두 번 롤백.
  여전히 출력은 자유 텍스트. 골든셋·에러 정책·스키마 개념 없음.
- 트랙 M: 아직 코드 변경 0. 골든셋 43건 2인 라벨 완료(불일치율 11.6%), 설계·ADR 승인.
